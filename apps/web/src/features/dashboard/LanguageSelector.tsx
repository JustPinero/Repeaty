// RED-phase stub — no selector.
// GREEN renders a select only when > 1 target language; persists choice to Zustand.
type Props = {
  targetLanguageCodes: string[];
};

export function LanguageSelector(_props: Props) {
  return null;
}
