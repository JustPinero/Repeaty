// RED-phase stub — wrong heading, no img.
// GREEN renders the Peaty illustration with meaningful alt + the greeting.
type Props = {
  displayName: string | null;
};

export function PeatyGreeting(_props: Props) {
  return <section><p>placeholder</p></section>;
}
