// RED stub — front shown but no flip behavior, no native_text reveal.
type Props = {
  targetText: string;
  nativeText: string;
  exampleTarget?: string;
  exampleNative?: string;
};

export function Flashcard(_props: Props) {
  return <div data-testid="flashcard">placeholder</div>;
}
