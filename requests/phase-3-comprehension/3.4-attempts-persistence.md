# Request 3.4 — `comprehension_attempts` Persistence + Card History

## Objective
Persist every comprehension attempt to the `comprehension_attempts` table (created in 0005 in Phase 1), and surface a per-card history view so the learner can see how their accuracy on a specific word has trended.

## Acceptance Criteria → Test Mapping

| Criterion                                                                       | Test                                                                       |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Each `submitResponse(response)` insert into `comprehension_attempts` carries `response_ms`, `correct`, `card_id`, `user_id` | `useComprehensionSession.test.ts` (mock supabase) |
| `correct = score ≥ 60` (i.e. bucket !== 'miss')                                 | same                                                                       |
| RLS: User B cannot read User A's attempts                                       | integration `comprehension-attempts-rls.test.ts`                           |
| Per-card history view shows last N attempts (newest first) with score + ms      | `CardComprehensionHistory.test.tsx`                                        |
| Empty history → friendly empty state                                            | same                                                                       |
| Card detail route `/app/decks/:deckId/cards/:cardId` mounts the history panel   | route + page test                                                          |
| Attempts list paginates beyond 20 rows (load more)                              | `CardComprehensionHistory.test.tsx > pagination`                           |

## Files to Touch

New:
- `apps/web/src/features/comprehension/CardComprehensionHistory.tsx` + `.test.tsx`
- `apps/web/src/pages/CardDetail.tsx`
- `apps/web/tests/integration/supabase/comprehension-attempts-rls.test.ts`

Updated:
- `apps/web/src/features/comprehension/useComprehensionSession.ts` — insert on each submit
- `apps/web/src/routes/index.tsx` — `/app/decks/:deckId/cards/:cardId`
- `apps/web/src/features/decks/DeckListItem.tsx` — link cards into the new detail page (or via a tertiary route — review/comprehension/details)
- `references/api-contracts.md` — note that no Edge Function is needed; insert is a direct supabase-js call gated by RLS

## Dependencies
- 3.2, 3.3

## Out of scope
- Pronunciation attempts history (Phase 4)
- Aggregate stats per deck (Phase 6 dashboard)
