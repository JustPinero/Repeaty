# Fix — Offline UX has no signal that work was captured

**Severity:** Medium (bughunt-phase-6 Med-2)
**Originating audit:** Phase 6 bughunt

This finding has the same scope and same fix as `fix-test-offline-banner.md` — viewed from the bughunt angle (UX gap that meaningfully affects Ben's first 30 minutes if connectivity is flaky) rather than the test-audit angle (component scoped but not built).

→ See `requests/phase-6-fixes/fix-test-offline-banner.md` for the acceptance criteria + files to touch.
