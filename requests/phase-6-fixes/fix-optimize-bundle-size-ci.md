# Fix — Bundle-size guard is honor-system (optimize duplicate)

**Severity:** Medium (optimize-phase-6 Med-2)
**Originating audit:** Phase 6 optimize

This finding has the same scope and same fix as `fix-test-bundle-size-ci.md`. Tracked separately so each audit's report links to a fix-request file as the audit-gate convention requires; the actual remediation is one commit covering both.

→ See `requests/phase-6-fixes/fix-test-bundle-size-ci.md` for the acceptance criteria + files to touch.
