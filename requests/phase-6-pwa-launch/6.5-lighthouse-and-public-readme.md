# Request 6.5 — Lighthouse ≥ 90 + Public README

## Objective
The kickoff exit criterion: Lighthouse ≥ 90 on PWA + Performance + Accessibility + Best Practices. README.md presentable enough to be a public link Justin can send a recruiter / friend / future contributor without embarrassment.

## Acceptance Criteria → Test Mapping

| Criterion                                                                       | Test                                                                       |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Lighthouse PWA score ≥ 90                                                       | manual audit (CI smoke optional)                                           |
| Lighthouse Performance ≥ 90 (mobile slow-4g profile)                            | manual                                                                     |
| Lighthouse Accessibility ≥ 90                                                   | manual + jsx-a11y already CI-blocking                                      |
| Lighthouse Best Practices ≥ 90                                                  | manual                                                                     |
| Main bundle gz ≤ 500 KB (per `references/deployment-landmines.md`)               | `pnpm build` artifact inspection                                           |
| `README.md` covers: what Repeaty is, screenshots, stack, how to run locally, how to contribute, link to architecture.md | manual review                                |
| `assets/peaty/peaty-poses.md` referenced from README so the mascot is discoverable | manual review                                                            |
| GitHub repo description + topics set                                            | manual via `gh repo edit`                                                  |

## RED Phase
- Add a `pnpm build:size` task that emits the gz size of `dist/assets/index-*.js` to stdout; threshold < 500 KB. CI runs it on main.

## GREEN Phase

1. **Lighthouse pass:** run `npx lighthouse http://localhost:5173 --view` against a production build, fix anything < 90. Common Phase-6 wins:
   - `<img>` width/height attributes on Peaty illustrations.
   - `loading="lazy"` on below-the-fold images.
   - Color contrast checks for the cream/green palette.
2. **Lazy-load the heavy routes** that the bundle-size audit (chore(6.0)) flagged: `AdminPage` + `GenerateLessonPage` via `React.lazy(() => import(...))`.
3. **README.md** rewrite with sections per the criteria above.
4. **GitHub repo description + topics** via `gh repo edit JustPinero/Repeaty --description "..." --add-topic pwa --add-topic language-learning --add-topic supabase`.

## Files to Touch

New:
- `scripts/build-size-report.sh`

Updated:
- `README.md`
- `apps/web/src/routes/index.tsx` — lazy import the AI routes.
- `apps/web/package.json` — `build:size` task.
- `.github/workflows/ci.yml` — bundle-size check on main.

## Out of scope
- A custom domain (post-launch).
- Marketing landing page (this is a beta sent to one friend).
