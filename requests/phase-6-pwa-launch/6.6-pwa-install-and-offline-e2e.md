# Request 6.6 — `pwa-install-and-offline` E2E

## Objective
The last unfilled `e2e-manifest.json` flow. Verifies the PWA manifest loads, the service worker registers, and the offline queue persists + replays a review rating across a simulated network drop.

## Acceptance Criteria → Test Mapping

| Criterion                                                                       | Test                                                                       |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Spec at `apps/web/tests/e2e/pwa-install-and-offline.spec.ts` exists             | filesystem                                                                  |
| Manifest fetch succeeds + has expected name + start_url                         | spec                                                                        |
| Service worker registers (Playwright awaits `navigator.serviceWorker.ready`)    | spec                                                                        |
| User offline: rate a flashcard → entry persists in IndexedDB                    | spec                                                                        |
| User reconnects: queue replays + the review row lands in `reviews`              | spec                                                                        |
| `e2e-manifest.json.flows.pwa-install-and-offline.status = "complete"`           | manifest                                                                    |

## RED Phase
- Author the spec; ensure CI's flows-list picks it up.

## GREEN Phase

1. **Playwright `browser.newContext({ offline: true/false })`** to simulate connectivity.
2. **IndexedDB inspection** via `page.evaluate(() => indexedDB.databases())` — confirm the queue table has the row.
3. **Reconnect → wait for replay** via the OfflineBanner's "last replay" timestamp updating.

## Files to Touch

New:
- `apps/web/tests/e2e/pwa-install-and-offline.spec.ts`

Updated:
- `e2e-manifest.json` — flip status.

## Dependencies
- 6.2 (manifest), 6.3 (service worker), 6.4 (offline queue).

## Out of scope
- Pronunciation/comprehension offline replay E2E — covered at the unit-hook level in 6.4; one canonical E2E (review queue) is enough.
