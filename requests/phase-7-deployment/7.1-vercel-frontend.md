# Request 7.1 — Vercel Frontend Deploy

## Objective
Get the Vite-built `apps/web/` SPA live on Vercel with a public URL. Cover the SPA-rewrite landmine (Vite + react-router-dom client-side routing 404s on direct URL hits without a wildcard rewrite — `references/deployment-landmines.md`), wire the two `VITE_*` env vars across all three Vercel environments, and verify the production build passes the `< 500 KB gz` budget that's already enforced locally via `pnpm build:size`.

## Acceptance Criteria → Test Mapping

| Criterion                                                                       | Test                                                                       |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `apps/web/vercel.json` sets the SPA rewrite + theme-color security headers     | manual review                                                              |
| `pnpm --filter @repeaty/web build` succeeds; `pnpm build:size` is green        | local + CI `build` job (already gated by chore-6.0 part 1)                 |
| Vercel project links to the GitHub repo with `Root Directory: apps/web`         | Vercel dashboard screenshot or `vercel project ls` output                  |
| `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` set for **Production, Preview, Development** | `vercel env ls`                                                  |
| First production deploy returns 200 on `/`                                       | `curl -sI https://<preview>.vercel.app/`                                   |
| Direct hit on `/login`, `/signup`, `/app/decks` returns 200 (SPA rewrite working) | same                                                                      |
| `manifest.webmanifest` fetches with valid JSON over HTTPS                        | `curl -s https://<preview>.vercel.app/manifest.webmanifest \| jq`         |
| Service worker registers in production (visible in DevTools → Application → SW) | manual                                                                     |

## RED Phase
- A smoke test in `scripts/post-deploy-smoke.sh` (lands in 7.3) hits the deployed URL and asserts the manifest + a couple of routes return 200. RED today because no URL exists.

## GREEN Phase

1. **`apps/web/vercel.json`:**
   ```json
   {
     "rewrites": [{ "source": "/(.*)", "destination": "/" }],
     "headers": [
       {
         "source": "/(.*)",
         "headers": [
           { "key": "X-Content-Type-Options", "value": "nosniff" },
           { "key": "X-Frame-Options", "value": "DENY" },
           { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
           { "key": "Permissions-Policy", "value": "microphone=(self), camera=()" }
         ]
       },
       {
         "source": "/manifest.webmanifest",
         "headers": [
           { "key": "Content-Type", "value": "application/manifest+json" },
           { "key": "Cache-Control", "value": "public, max-age=3600" }
         ]
       }
     ]
   }
   ```
2. **Vercel project setup (one-time, manual):**
   - `vercel login` (or via the dashboard).
   - `vercel link` from repo root → "Link to existing project? **N**" → "What's your project's name? **repeaty**" → "In which directory is your code located? **apps/web**".
   - `vercel env add VITE_SUPABASE_URL production` (paste the cloud URL), then `preview`, then `development`. Same for `VITE_SUPABASE_ANON_KEY`.
   - First deploy: `vercel --prod`.
3. **Build settings (auto-detected from package.json#packageManager + apps/web):**
   - Framework: Vite.
   - Root directory: `apps/web`.
   - Build command: `pnpm install && pnpm build` (Vercel auto-detects pnpm via packageManager).
   - Output: `dist/`.
4. **Verify** with `curl` against the resulting URL.

## Files to Touch

New:
- `apps/web/vercel.json`

Updated:
- `references/deployment-landmines.md` — confirm SPA rewrite snippet matches what we're shipping.
- `README.md` — production URL once we have one.

## Out of scope
- Custom domain (post-launch).
- Vercel Analytics / Speed Insights (free tier; add later if useful).
- Auto-deploy from `main` (manual `vercel --prod` for v1; CI auto-deploy is Phase-8 polish).
